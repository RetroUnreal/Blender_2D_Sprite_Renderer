Renders a sprite from the camera on each frame of every NLA track on the current selected armature, so make sure to push your animations to the NLA tracks and have your armature selected before running the script

Just run as a script in the scripting tab in blender.
